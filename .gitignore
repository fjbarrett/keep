node_modules
.next
.env*.local
.DS_Store
tsconfig.tsbuildinfo
*.tsbuildinfo
/takeout/

# Vercel CLI project link
.vercel

# Local working/handover notes — never publish to the public repo
HANDOVER.md
docs/saved-tasks.md

# Vendored pdfjs worker (copied from node_modules at build time)
/public/pdf.worker.min.mjs
